[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trading-project"
version = "0.1.0"
description = "Research-grade XAUUSD M5 trading research project"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.1,<3.0",
  "TA-Lib>=0.4.28,<0.5.0",
  "pytest>=8.0,<9.0"
]
